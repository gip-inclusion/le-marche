{ "deploy": {
    "module": "lemarche.config.wsgi:application",
    "managetasks":
        [
            "migrate --no-input",
            "collectstatic --no-input",
            "compress --force"
        ]
    }
}
